<?xml version="1.0" encoding="utf-8"?>
<!-- The notification icon is referenced only by name from Dart, so R8
     resource shrinking cannot see it and would strip it from release builds. -->
<resources xmlns:tools="http://schemas.android.com/tools"
    tools:keep="@drawable/ic_notification" />